# Railway config — used by nexus-dashboard service
# Bot services override NIXPACKS_BUILD_CMD and NIXPACKS_START_CMD via env vars
[build]
builder = "nixpacks"

[deploy]
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
