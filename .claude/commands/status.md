Give a quick status report of the NexusTradeAI project:

1. Run `gh run list --limit 3` to check recent CI/CD status
2. Run health checks on all 3 bots + strategy bridge
3. Check `git log --oneline -5` for recent changes
4. Report current version (look for latest vXX.X in commit messages)
5. Check for any open GitHub issues: `gh issue list --limit 5`

Keep the report concise — bullet points, not paragraphs.
