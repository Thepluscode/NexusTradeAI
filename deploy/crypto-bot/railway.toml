[build]
builder = "dockerfile"
dockerfilePath = "deploy/crypto-bot/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
